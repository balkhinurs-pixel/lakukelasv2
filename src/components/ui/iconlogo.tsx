import type { SVGProps } from "react";

export function IconLogo(props: SVGProps<SVGSVGElement>) {
  // This component is no longer used and can be removed or left empty.
  // Returning null to prevent any rendering.
  return null;
}
